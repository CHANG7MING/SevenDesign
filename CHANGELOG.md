# Changelog

## Unreleased

- Rebased the skill documentation on the latest online SevenDesign structure, including the unified `seven-design` router, upstream craft skills, Team Mode routing, and `personal-site` prototype.
- Added React Bits as a first-class public component and motion integration layer with JS/TS and CSS/Tailwind variant routing.
- Added `micro`, `system`, and `signature` motion tiers, motion budgets, reduced-motion requirements, and a product-level motion composition review bridge.
- Kept `review-animations` as the strict code-level motion authority and linked it from the new React Bits workflow.
- Documented the boundary between public React Bits usage and unavailable Pro source, private assets, and gated templates.
- Added a pinned public React Bits registry snapshot, provenance and license records, and six materialized public component variants under `skills/react-bits/catalog/components/`.
- Added the official read-only `find-animation-opportunities` and `improve-animations` skills, including their animation filtering and audit-to-plan routing.
- Added an intelligent React Bits selection protocol, machine-readable decision matrix, reasoned recommendation format, and a local ranking helper instead of expanding the catalog indiscriminately.
- Added Vue Bits as a secondary public implementation path with a React-default framework resolution protocol and two selective Vue source snapshots.
- Added token-budget rules and a compact registry search helper so catalog size does not become default context cost.

## 0.1.0

- Initial public-ready library structure
- Root `DESIGN.md` system
- Semantic token layer
- Brand recipes
- Page archetypes
- Component recipes
- Implementation examples
- Quickstart, prompts, starter kits, workflows, and skill-style entrypoints
